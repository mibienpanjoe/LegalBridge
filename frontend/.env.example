# Backend API URL — set this to your Railway/Render deployment URL in production.
# Leave unset (or set to http://localhost:8080) for local development.
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app
